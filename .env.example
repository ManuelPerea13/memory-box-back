# Django
SECRET_KEY=your-secret-key-change-in-production
DEBUG=True
# Only if DEBUG=False: comma-separated list of allowed hosts
# ALLOWED_HOSTS=localhost,127.0.0.1,192.168.88.100

# PostgreSQL (Docker: DB_HOST=db)
POSTGRES_DB=memory_box
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432
